<UserControl x:Class="CHeroBot.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CHeroBot.Views"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:SearchViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>

        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Center">
            <TextBlock Text="{Binding Status}" TextWrapping="Wrap" HorizontalAlignment="Center" VerticalAlignment="Center" />
            <Button x:Name="CmdClick" Content="Cancel" Margin="0,30,0,0" HorizontalAlignment="Center" />
        </StackPanel>
        
    </Grid>
</UserControl>
